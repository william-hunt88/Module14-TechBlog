<article class="post">
    <div class="title">
        {{title}}
        {{content}}
    </div>
    <div class="meta">
        {{user.username}}
        {{created_at}}
        <a href="/post/{{id}}">{{comments.length}} {{comments.length}}</a>
    </div>
</article>
<script src="/javascript/comment.js"></script>